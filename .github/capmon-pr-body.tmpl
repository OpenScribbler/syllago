# capmon: Capability Drift Detected

This pull request was opened automatically by the capability monitor pipeline.

## What changed

The pipeline detected changes between the previously recorded provider capabilities
and the current state of the provider's documentation source. See the diff below
for field-level changes and structural drift (new or removed landmarks).

**Important:** This PR contains machine-extracted data. It has not been verified
by a human. Do not merge without reviewing each changed field against the linked
source URL.

## How to review

1. Open the source URL listed in `docs/provider-capabilities/<slug>.yaml` under `references`.
2. Verify each changed field value against the live documentation.
3. If the change is correct, approve and merge.
4. If the pipeline made an error, close this PR and file an issue with the `capmon` label.

## Pipeline output is not ground truth

The capability monitor extracts text from documentation pages. Documentation pages
can be incomplete, ambiguous, or temporarily incorrect. Always verify against the
provider's official release notes or API reference before treating a detected
change as authoritative.

---

*Generated by [syllago capmon](https://github.com/OpenScribbler/syllago). Do not edit the body of this PR — changes will be overwritten on the next run.*
