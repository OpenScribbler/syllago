---
name: smoke-agent
description: Test agent used by tests/smoke/.
---

# Smoke Agent

Self-contained agent used by `tests/smoke/`. Asserts that `syllago loadout
apply` symlinks `AGENT.md` to `<install-dir>/<name>.md` for the agent
content type.
