# Smoke Rules

Test rule used by `tests/smoke/`. Asserts that `syllago loadout apply` can
symlink a provider-specific rules item into the target provider's rules
directory.

Do not consume this content outside the smoke test suite.
