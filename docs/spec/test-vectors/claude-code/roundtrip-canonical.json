{
  "_comment": "Round-trip test: expected canonical form after decoding roundtrip-source.json (Claude Code native). Encoding this back to Claude Code MUST produce output structurally equivalent to roundtrip-source.json. Decode mappings: PreToolUse -> before_tool_execute, Bash -> shell (tool vocabulary reverse lookup), SessionStart -> session_start.",
  "spec": "hooks/0.1",
  "hooks": [
    {
      "event": "before_tool_execute",
      "matcher": "shell",
      "handler": {
        "type": "command",
        "command": "./audit-shell.sh",
        "timeout": 10
      },
      "blocking": true
    },
    {
      "event": "session_start",
      "handler": {
        "type": "command",
        "command": "./init.sh"
      },
      "blocking": false
    }
  ]
}
