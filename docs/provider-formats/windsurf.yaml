provider: windsurf
last_fetched_at: "2026-04-11T00:00:00Z"
last_changed_at: "2026-04-11T00:00:00Z"
generation_method: human-edited

content_types:
  skills:
    status: supported
    sources:
      - uri: "https://docs.windsurf.com/windsurf/cascade/skills.md"
        type: documentation
        fetch_method: md_url
        content_hash: ""
        fetched_at: "2026-04-11T00:00:00Z"
    canonical_mappings:
      display_name:
        supported: true
        mechanism: "yaml frontmatter key: name (required)"
        confidence: confirmed
      description:
        supported: true
        mechanism: "yaml frontmatter key: description (required)"
        confidence: confirmed
      user_invocable:
        supported: true
        mechanism: "@mention by skill name in Cascade input (e.g., @deploy-to-production)"
        confidence: confirmed
      project_scope:
        supported: true
        mechanism: "directory: .windsurf/skills/<skill-name>/SKILL.md"
        paths:
          - ".windsurf/skills/<skill-name>/SKILL.md"
          - ".agents/skills/<skill-name>/SKILL.md"
          - ".claude/skills/<skill-name>/SKILL.md"
        confidence: confirmed
      global_scope:
        supported: true
        mechanism: "directory: ~/.codeium/windsurf/skills/<skill-name>/SKILL.md"
        paths:
          - "~/.codeium/windsurf/skills/<skill-name>/SKILL.md"
          - "~/.agents/skills/<skill-name>/SKILL.md"
        confidence: confirmed
      shared_scope:
        supported: true
        mechanism: "OS-specific system path (Enterprise IT-deployed, read-only)"
        confidence: confirmed
      canonical_filename:
        supported: true
        mechanism: "fixed filename SKILL.md required in each skill directory"
        confidence: confirmed
    provider_extensions: []
    loading_model: "Progressive disclosure: only name+description shown to model by default; full SKILL.md content loaded when Cascade invokes the skill or user @mentions it."
    notes: "The windsurf.md format doc incorrectly listed Skills as unsupported — Windsurf has a full file-based skills system. Windsurf implements the agentskills.io open standard. Also discovers skills in .agents/skills/ and .claude/skills/ for cross-agent compatibility."
