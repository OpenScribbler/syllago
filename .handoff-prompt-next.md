# Next Session Prompt

Continue the provider accuracy overhaul for syllago. Last session we completed exhaustive research on all 11 providers, ran audits, and fixed 38 of 44 audit beads (14 commits, +2613/-402 lines). All tests pass on main.

## Priority Work

### 1. Remaining 6 syllago audit beads (P3)

These are the last fixes from the provider audit. Run `bd ready` to see them. Key ones:

- **syllago-qumq**: Fix hook matcher regex handling — matchers like `Edit|Write` get passed to TranslateTool as a single string instead of translating each component. Also consider updatedInput preservation (syllago-ij3k depends on this).
- **syllago-6vz1**: Add Gemini CLI hook events (BeforeModel, AfterModel, BeforeToolSelection) to HookEvents — Gemini-only events that currently get silently dropped on import.
- **syllago-w583**: Fix Gemini CLI provider — commands path (.gemini/ vs .gemini/commands/) and skills discovery (.agents/skills/ alternate path).
- **syllago-6fb4**: Add OAuth support to Claude Code MCP config.
- **syllago-veab**: Fix Cline provider — global rules path, platform-aware MCP path.
- **syllago-puom**: Add Windsurf skills and MCP converters.

### 2. Quick win

- **syllago-2cg9**: Add MultiEdit, LS, NotebookRead, KillBash to ToolNames (CC-only, empty maps). 5 minutes.

### 3. Kitchen sink repo (15 beads)

Switch to `../syllago-kitchen-sink/` and run `bd ready` there. Fix in priority order:
- P0: Kiro agent JSON→markdown (ks-rt3), Codex golden tool names (ks-3nx)
- P1: Copilot MCP path (ks-e9a), Copilot hooks dir (ks-2zx), OpenCode skills path (ks-pyu)
- P2: Missing fixtures for 6 providers
- P3: Spec accuracy fixes

The full audit is at `docs/providers/audit/kitchen-sink-repo.md`.

### 4. Original 3 feature beads (from the blog post analysis)

- **syllago-11cr**: Canonical format versioning — add `format: syllago/v1` to metadata
- **syllago-3lrt**: Tool map completeness audit — largely done by the mega toolmap fix, verify and close
- **syllago-hpld**: Private repo content locking — new feature, needs design

## Reference

- Provider research: `docs/providers/<provider>/` (4 files each × 11 providers)
- Audit reports: `docs/providers/audit/<provider>.md` (11 files)
- Consolidated findings: `docs/providers/audit/FINDINGS.md` (78 findings)
- Kitchen sink audit: `docs/providers/audit/kitchen-sink-repo.md`
- Hook deep-dive: `docs/providers/audit/hooks.md`
- Blog post that started this: https://dacharycarey.com/2026/03/20/why-platform-shouldnt-own-open-spec/
